#조합
result = list(itertools.combinations((["1","2","3","4"]),2))
print("**경우의 수 : %s개" % len(result))
print(result)
#선택하는 경우의 수 combinations 함수를 통해 이용할 수 있다.
#결과
#경우의 수 : 6개
[(1, 2), (1, 3), (1, 4), (2, 3), (2, 4), (3, 4)]
